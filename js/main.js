
// Main JavaScript file for the homepage
document.addEventListener('DOMContentLoaded', function() {
  console.log('Homepage loaded');
  
  // Load featured products (moved to featuredProducts.js)
  // Load testimonials (moved to testimonialSection.js)
  // Load Instagram posts (moved to instagramSection.js)
});
